apiVersion: skaffold/v1beta11
kind: Config
build:
  tagPolicy:
    sha256: {}
  artifacts:
  - image: mine-support
    sync:
      manual:
      - src: '**/*.js'
        dest: .
deploy:
  helm:
    releases:
    - name: skaffold-mine-support
      chartPath: helm
      #wait: true
      valuesFiles:
      - helm/dev-values.yaml
      values:
        image: mine-support
